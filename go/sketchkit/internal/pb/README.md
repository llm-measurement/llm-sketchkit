# Protobuf Codegen Target

Generated Go protobuf code for `spec/sketches.proto` is committed in this
directory once `scripts/regenerate_protos.sh` is run with `protoc` and
`protoc-gen-go` available.

The generated package name is `sketchpb`.
